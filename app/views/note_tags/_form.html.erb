<%= form_with(model: note_tag, local: true) do |form| %>
  <% if note_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note_tag.errors.count, "error") %> prohibited this note_tag from being saved:</h2>

      <ul>
      <% note_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :note_tag_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
